This folder contains the raw sensor data collected from the multimodal glove system in the project. Each recording includes five flex-sensor channels (`a0`-`a4`), two IMU attitude channels (`roll` and `pitch`), and timestamps. These raw data were used as the basis for sensor analysis, communication experiments, threshold-based labelling, sliding-window generation, and subsequent model training.
